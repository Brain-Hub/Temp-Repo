import { Boxes, Box } from "@/core/config/BoxTypes";

const box: Box = {
  id: "",
};

const boxes: Boxes = {
  id: "",
  data: { listBoxes: { items: [{ id: "", updatedAt: "" }] } },
  box: [box],
};

export default boxes;
